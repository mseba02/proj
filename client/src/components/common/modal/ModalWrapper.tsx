import React, { FC } from 'react'
import { IModalBodyProps } from '../../../interfaces/modal'

const ModalWrapper: FC<React.PropsWithChildren<IModalBodyProps>> = ({
  children,
  triggerTitle,
}) => {
  return <div></div>
}

export default ModalWrapper
